import { Text, View } from "react-native"

import React from "react"

const Home = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-center bg-blue-400 p-4 rounded-2xl font-medium">
        React Native App with Expo Router, TypeScript and NativeWind for
        TailwindCSS
      </Text>
    </View>
  )
}

export default Home
